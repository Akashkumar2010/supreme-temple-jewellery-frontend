import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminProducts = () => {
  return (
    <Box>
      <Typography variant="h6">Admin Products Management</Typography>
      {/* Add CRUD functionality here */}
    </Box>
  );
};

export default AdminProducts;
